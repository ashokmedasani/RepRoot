import{Component}from'@angular/core';import{FormsModule}from'@angular/forms';import{Router}from'@angular/router';import{AdminPortalApiService}from'@core/api/admin-portal-api.service';import{formatApiError}from'@shared/utils/ui-helpers';
@Component({selector:'app-admin-change-password',standalone:true,imports:[FormsModule],template:`<main><section><p class="eyebrow">Staff security</p><h1>Choose your private password</h1><p>Your temporary password worked. Before accessing RepRoot operations, replace it with a password known only to you.</p><form (ngSubmit)="save()"><label>Temporary/current password<input type="password" name="current" [(ngModel)]="current" required></label><label>New password<input type="password" name="password" [(ngModel)]="password" minlength="10" required></label><label>Confirm new password<input type="password" name="confirm" [(ngModel)]="confirm" required></label>@if(message){<p class="message">{{message}}</p>}<button [disabled]="loading">{{loading?'Securing account…':'Change password and continue'}}</button></form></section></main>`,styles:[`main{display:grid;min-height:100vh;place-items:center;padding:1rem;background:var(--app-bg)}section{width:min(30rem,100%);border:1px solid var(--app-border);border-radius:1rem;padding:2rem;background:var(--app-surface);box-shadow:var(--app-shadow-lg)}h1{margin:.2rem 0}p{color:var(--app-muted)}form,label{display:grid;gap:.5rem}form{gap:1rem;margin-top:1.3rem}input{min-height:2.7rem;border:1px solid var(--app-border);border-radius:.6rem;padding:.5rem;background:var(--app-surface);color:var(--app-text)}button{border:0;border-radius:.65rem;padding:.75rem;background:var(--app-primary);color:#fff;font-weight:800}.message{padding:.6rem;border-radius:.5rem;background:var(--app-primary-soft)}`]})
export class AdminChangePasswordComponent{current='';password='';confirm='';message='';loading=false;constructor(private api:AdminPortalApiService,private router:Router){}save():void{if(!this.current||!this.password||this.password!==this.confirm){this.message='Enter matching passwords.';return}this.loading=true;this.api.changePassword(this.current,this.password,this.confirm).subscribe({next:r=>{this.api.clearSession();window.sessionStorage.setItem('admin-password-notice',r.message);void this.router.navigate(['/admin-portal/login'])},error:e=>{this.loading=false;this.message=formatApiError(e,'Password could not be changed.')}})}}
