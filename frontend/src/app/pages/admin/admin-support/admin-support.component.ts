import{DatePipe}from'@angular/common';import{Component,OnInit}from'@angular/core';import{FormsModule}from'@angular/forms';import{AdminPortalApiService}from'@core/api/admin-portal-api.service';import{AdminPageShellComponent}from'@admin-shared/admin-page-shell/admin-page-shell.component';import{formatApiError}from'@shared/utils/ui-helpers';
@Component({selector:'app-admin-support',standalone:true,imports:[AdminPageShellComponent,FormsModule,DatePipe],template:`<app-admin-page-shell title="Support Workspace" subtitle="Resolve individual cases through consent, controlled actions and complete audit history." activeSection="support"><form page-actions class="filters" (ngSubmit)="load()"><input name="search" [(ngModel)]="search" placeholder="ID, person, email or subject"><select name="role" [(ngModel)]="role" (change)="load()"><option value="">All reporters</option><option value="professional">Professionals</option><option value="client">Clients</option></select><select name="status" [(ngModel)]="status" (change)="load()"><option value="">All statuses</option><option value="submitted">Submitted</option><option value="open">Open</option><option value="under_review">Under review</option><option value="waiting_for_user">Waiting for user</option><option value="resolved">Resolved</option><option value="closed">Closed</option></select><button>Search</button></form>
@if(message){<p class="message">{{message}}</p>}<section class="stats"><article><small>Matching tickets</small><strong>{{count}}</strong></article><article><small>Active queue</small><strong>{{activeCount}}</strong></article></section><section class="queue">@for(item of incidents;track item.incident_id){<button [class.selected]="selected?.incident_id===item.incident_id" (click)="select(item)"><div><strong>{{item.subject}}</strong><span>{{item.incident_id}} · {{item.reporter_name}} · {{item.reporter_role}}</span></div><span>{{item.priority}}</span><span>{{item.status}}</span><time>{{item.updated_at|date:'medium'}}</time></button>}@empty{<p>No support incidents match these filters.</p>}</section>
@if(selected){<section class="workspace"><header><div><p class="eyebrow">{{selected.incident_id}}</p><h2>{{selected.subject}}</h2><p>{{selected.description}}</p></div><button (click)="selected=undefined">Close</button></header><div class="history">@for(msg of selected.messages||[];track msg.id){<article><strong>{{msg.author_name}} · {{msg.author_type}}</strong><p>{{msg.body}}</p><small>{{msg.created_at|date:'medium'}}</small></article>}</div><h3>Ticket workflow</h3><div class="actions"><label>Status<select [(ngModel)]="action.status"><option value="open">Open</option><option value="under_review">Under review</option><option value="waiting_for_user">Waiting for user</option><option value="resolved">Resolved</option><option value="closed">Closed</option></select></label><label>Priority<select [(ngModel)]="action.priority"><option value="low">Low</option><option value="normal">Normal</option><option value="high">High</option><option value="urgent">Urgent</option></select></label><label class="wide">Reply to user<textarea [(ngModel)]="action.reply"></textarea></label><label class="wide">Internal note<textarea [(ngModel)]="action.internal_note"></textarea></label><label><input type="checkbox" [(ngModel)]="action.assign_to_me"> Assign to me</label><button class="primary" (click)="save()">Update ticket</button></div>
<section class="consent"><h3>Consent-based account assistance</h3><p>Without a verified consent reference, this records a pending request only. Verified consent activates access for one hour.</p><div class="actions"><label>Scope<select [(ngModel)]="access.scope"><option value="metadata">Account metadata</option><option value="module">Specific module</option><option value="readonly">Read-only view</option></select></label><label>Module<input [(ngModel)]="access.module" placeholder="For module scope"></label><label class="wide">Investigation reason<textarea [(ngModel)]="access.reason"></textarea></label><label class="wide">Verified consent reference<input [(ngModel)]="access.consent_reference" placeholder="User reply or approval reference"></label><button class="primary" (click)="requestAccess()">Record access request</button></div>@if(activeAccessId){<div class="approved"><strong>Temporary consent active</strong><span>{{activeAccessId}}</span></div><div class="actions"><label>Controlled action<select [(ngModel)]="controlled.action"><option value="end_sessions">End active sessions</option><option value="unlock_account">Unlock account</option></select></label><label>Action reason<input [(ngModel)]="controlled.reason"></label><button class="primary" (click)="runControlledAction()">Run audited action</button></div>}</section></section>}</app-admin-page-shell>`,styles:[`.filters{display:flex;gap:.4rem}.filters input,.filters select,.filters button,input,select,textarea{border:1px solid var(--app-border);border-radius:.6rem;padding:.5rem;background:var(--app-surface);color:var(--app-text)}.message,.queue,.workspace{border:1px solid var(--app-border);border-radius:1rem;padding:1rem;background:var(--app-surface);margin-bottom:1rem}.stats{display:grid;grid-template-columns:repeat(2,12rem);gap:.7rem;margin-bottom:1rem}.stats article{display:grid;border:1px solid var(--app-border);border-radius:.8rem;padding:.8rem;background:var(--app-surface)}.stats strong{font-size:1.5rem}.queue>button{width:100%;display:grid;grid-template-columns:1fr auto auto auto;gap:.7rem;text-align:left;padding:.75rem;border:0;border-bottom:1px solid var(--app-border);background:transparent;color:var(--app-text)}.queue button div{display:grid}.queue span,.queue time{color:var(--app-muted);font-size:.78rem}.selected{background:var(--app-primary-soft)!important}.workspace header{display:flex;justify-content:space-between}.history article{margin:.6rem 0;border-left:3px solid var(--app-primary);padding:.6rem;background:var(--app-surface-soft)}.actions{display:grid;grid-template-columns:1fr 1fr;gap:.7rem}.actions label{display:grid;gap:.3rem}.wide{grid-column:1/-1}.primary{border:0;border-radius:.6rem;padding:.7rem;background:var(--app-primary);color:#fff;font-weight:800}.consent{margin-top:1.2rem;border-top:1px solid var(--app-border);padding-top:1rem}.approved{display:flex;justify-content:space-between;margin:1rem 0;padding:.7rem;border-radius:.6rem;background:#dcfce7;color:#166534}@media(max-width:800px){.queue>button,.actions{grid-template-columns:1fr}.wide{grid-column:auto}}`]})
export class AdminSupportComponent implements OnInit{search='';role='';status='';incidents:any[]=[];selected:any;count=0;activeCount=0;message='';activeAccessId='';action={status:'under_review',priority:'normal',reply:'',internal_note:'',assign_to_me:false};access={scope:'metadata',module:'',reason:'',consent_reference:''};controlled={action:'end_sessions',reason:''};constructor(private api:AdminPortalApiService){}ngOnInit(){this.load()}load(){this.api.getSupportIncidents({search:this.search,reporter_role:this.role,status:this.status}).subscribe({next:r=>{this.incidents=r.results;this.count=r.count;this.activeCount=r.active_count},error:e=>this.message=formatApiError(e,'Support queue could not be loaded.')})}select(i:any){this.selected=i;this.activeAccessId='';this.action={status:i.status,priority:i.priority,reply:'',internal_note:'',assign_to_me:false}}save(){this.api.updateSupportIncident(this.selected.incident_id,this.action).subscribe({next:r=>{this.selected=r.incident;this.message=r.message;this.load()},error:e=>this.message=formatApiError(e,'Ticket could not be updated.')})}requestAccess(){this.api.requestSupportAccess(this.selected.incident_id,this.access).subscribe({next:r=>{this.message=r.message;this.activeAccessId=r.status==='approved'?r.access_id:''},error:e=>this.message=formatApiError(e,'Access request could not be recorded.')})}runControlledAction(){this.api.runSupportAction(this.selected.incident_id,{...this.controlled,access_id:this.activeAccessId}).subscribe({next:r=>this.message=r.message,error:e=>this.message=formatApiError(e,'Controlled action could not be completed.')})}}
