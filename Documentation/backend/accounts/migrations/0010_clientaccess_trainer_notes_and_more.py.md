# backend/accounts/migrations/0010_clientaccess_trainer_notes_and_more.py

Adds `trainer_notes` (private text, trainer-only) and `trainer_notes_updated_at` to `ClientAccess` for the client-profile Trainer Notes card.
