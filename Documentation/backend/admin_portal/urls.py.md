# backend/admin_portal/urls.py

Routes `/api/admin/login/`, `logout/`, `me/`, `dashboard/`, `finance/`, and `audit-logs/`. Backend permission enforcement is mandatory even when the frontend hides a navigation item.
