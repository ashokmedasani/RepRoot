# backend/admin_portal/migrations/0002_seed_roles_permissions.py

Seeds least-privilege defaults for Super Admin, Support Admin, Support Agent, Operations Admin, and Read-Only Analyst. Finance is a separate permission group (`admin.finance.view` and `admin.finance.export`).
