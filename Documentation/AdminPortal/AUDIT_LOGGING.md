# Audit Logging

Administrative actions create immutable records with staff/role snapshots, permission used, action, target type, safe target display, outcome, timestamp, IP, user agent, and correlation ID.

Trainer logs use `username · TRN-reference`. Client logs use `trainer_username:CL-reference`. Both relationship IDs and snapshots are stored. Passwords, tokens, temporary passwords, and payment credentials are prohibited.
