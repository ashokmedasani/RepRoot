# Finance Module

Finance is a dedicated page and permission section, not a Dashboard card. The initial page reports verified ledger data only: gross revenue, completed/pending/failed transactions, refunds, and recent entries. With no billing integration configured, it safely displays zero records and “Ready for integration.”

Permissions: `admin.finance.view`, `admin.finance.export`.
