# frontend/src/app/pages/trainer/trainer-references/trainer-references.component.html

References Library template: category sidebar with counts, reference table with quick-add buttons, detail panel (YouTube iframe, image preview, info grid, tags), and modal editors. Category select binds category IDs; images render from server `file_url`.
