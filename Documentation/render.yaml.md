# render.yaml

## 2026-07-13 production audit

Defines the PostgreSQL database, Django API service, and Angular static site. Production environment inputs include secure cookies/HSTS, SMTP credentials, shared Redis cache, and optional S3-compatible durable media storage. Secrets remain external to source control.
